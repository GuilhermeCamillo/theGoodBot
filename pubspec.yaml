name: thegoodbotps
description: Arquivo para a avaliação de ps

publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^1.3.0
  flutter_dialogflow: ^0.1.0
  url_launcher: ^5.4.5
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
  assets:
    - assets/credentials.json
    - assets/bot.png
    - assets/guia.jpg
    - assets/help.png
